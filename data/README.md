# Data Directory

Local dataset files belong here and are ignored by Git.

- `raw/`: original Amazon Berkeley Objects metadata and images.
- `interim/`: validation reports and skipped-row diagnostics.
- `processed/`: prepared metadata used for embedding generation.

Do not commit raw data, copied product images, or processed dataset outputs.
