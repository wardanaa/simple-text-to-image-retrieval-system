# Change Notes

Every logical repository change needs one Markdown file in this directory.

Use sequential identifiers such as `0001`, `0002`, and `0003`, and descriptive kebab-case
filenames. Update `CHANGELOG.md` whenever a change note is added.
