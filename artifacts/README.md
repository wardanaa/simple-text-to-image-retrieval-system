# Artifacts

Generated model artifacts belong here and are ignored by Git.

The embedding index uses:

- `embeddings/image_embeddings.npy`
- `embeddings/metadata.csv`
- `embeddings/index_manifest.json`

Regenerate these files from local dataset inputs instead of committing them.
